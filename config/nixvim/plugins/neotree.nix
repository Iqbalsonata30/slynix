{
  plugins = {
    neo-tree = {
      enable = true;
      settings = {
        enable_git_status = true;
        enable_diagnostics = true;
        filesystem.filtered_items.visible = true;
      };
    };
  };
}
